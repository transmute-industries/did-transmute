import {
  sign,
  verify,
  actor, // kill me

  // encrypt,
  // decrypt,
  resolve,
  dereference,
} from "./actions";

const jwt = {
  sign,
  verify,
  actor,
  // encrypt,
  // decrypt,
  resolve,
  dereference,
};

export default jwt;
