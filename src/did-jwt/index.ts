import {
  sign,
  verify,
  actor,
  resolve,

  // dereference,

  // encrypt,
  // decrypt,
} from "./actions";

const jwt = {
  sign,
  verify,
  actor,
  // encrypt,
  // decrypt,
  resolve,

  // dereference,
};

export default jwt;
