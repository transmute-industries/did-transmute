import {
  exportable,
  resolve,
  dereference,
  fromPrivateKey,
  fromDids,
} from "./actions";

const web = {
  exportable,

  resolve,
  dereference,

  fromPrivateKey,
  fromDids,
};

export default web;
